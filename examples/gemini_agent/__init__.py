"""Gemini demo target — Vertex Reasoning Engine via contract.

This package ships a contract that points at a Vertex AI reasoning
engine (Agent Engine). No local Python agent code — the agent lives in
your GCP project. For an in-process Gemini-backed demo, see
``examples.langgraph.simple_chatbot`` (LangGraph + Gemini via the
AI Studio OpenAI shim).
"""
