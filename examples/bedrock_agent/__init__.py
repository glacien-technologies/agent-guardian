"""AWS Bedrock Agent demo target — contract-only.

Bedrock agents are scanned via a target contract that declares the
``bedrock_agent`` transport. There is no local Python agent code in this
directory — the agent lives in your AWS account. This package ships the
contract template, a README walking through agent creation, and a sample
scan output.
"""
